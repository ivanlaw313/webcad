{
  "summary": "Audit webcad UI files vs verified Fusion 360 GUI structure, produce prioritized parity spec",
  "agentCount": 7,
  "logs": [],
  "result": "# webcad → Fusion 360 GUI Parity — Phased Implementation Spec\n\n**Target:** Make webcad's GUI structurally identical to Fusion 360 (light theme, 2560×1440 reference screenshots) while keeping ALL existing functionality reachable and ALL labels in Chinese (Fusion layout, Chinese text, keep existing `tip` strings).\n\n**Key files:**\n`C:\\ClaudeCode\\webcad\\src\\ribbon.ts` · `C:\\ClaudeCode\\webcad\\src\\components\\Ribbon.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\Viewport.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\BrowserTree.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\Timeline.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\CSketch.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\CommandPalette.tsx` · `C:\\ClaudeCode\\webcad\\src\\components\\SketchLayer.tsx` · `C:\\ClaudeCode\\webcad\\src\\store.ts` · `C:\\ClaudeCode\\webcad\\src\\App.tsx` · `C:\\ClaudeCode\\webcad\\src\\index.css` · `C:\\ClaudeCode\\webcad\\src\\icons.tsx`\n\n**New files:** `src/components/CommandDialog.tsx`, `src/components/MarkingMenu.tsx`, `src/components/SketchPalette.tsx` (Phase 2), `src/components/FileMenu.tsx` (or inline in Ribbon.tsx).\n\n**Conflict resolutions (where audit reports disagreed):**\n- **CONFIGURE group**: included (Report 1's approach wins over Report 6's \"skip\") — populated with the existing `params` command, so it is not hollow.\n- **S key**: rebound to the shortcuts/search box per verified Fusion behavior (Report 5 S5 wins over Report 6's \"leave as is\"); sketch keeps its ribbon button + `Shift+S`.\n- **Group dropdown direction**: opens DOWN over the canvas (all reports agree; current opens up).\n- **Accent**: keep light theme, accent → `#1a73e8` family, selection highlight → orange (Report 6 GAP-3).\n- **FORM workspace**: NOT built (no T-spline kernel — don't ship a hollow tab). The contextual-tab *mechanism* (Phase 2) is built exactly like Fusion's FORM/SKETCH pattern so FORM can be added later.\n\n---\n\n## Phase 1 — Ribbon core (highest visual impact)\n\n### 1.1 Data model: quick icons, dividers, submenus\n**File:** `src/ribbon.ts`\n- Extend `Tool` (line 1): add `quick?: boolean`, `sep?: boolean` (divider rendered *before* this row), `children?: Tool[]` (submenu ▸).\n- Mark `quick: true` mirroring Fusion's strips:\n  - CREATE (ribbon.ts:11-51): `sketch`, `extrude` (E), `revolve`, `hole` (H), `pattern`, `loft`, `sweep` (7 icons).\n  - MODIFY (:52-68): `presspull` (Q), `fillet` (F), `chamfer`, `shell`, `combine`, `move` (M), `appearance` (A).\n  - All other panels & workspaces: first ≤4 tools (or existing `big` ones) get `quick: true`.\n- Insert `sep: true` at Fusion's CREATE menu section boundaries: after the sketch/form block; after Loft/Rib block; after Hole/Thread; after primitives (box…pipe); before the Pattern/Mirror/Thicken block. Same for MODIFY: after Silhouette-Split-equivalents; after Move/Align; after materials; after delete.\n- Convert `pattern`/`cpattern`/`pathpattern` into one parent `{ id:'pattern', label:'阵列', children:[…] }` with ▸ submenu (Fusion: \"Pattern ▸\"). Children keep their original ids so `runCommand` is untouched.\n- **No command may be deleted** — anything not `quick` stays in the dropdown.\n\n### 1.2 Render quick row + dropdown-only commands (fixes ribbon overflow/clipping)\n**File:** `src/components/Ribbon.tsx` (:249-261), `src/index.css` (:79-101)\n- Panels row renders `p.tools.filter(t => t.quick)` inline (fallback `p.tools.slice(0,6)` if a panel has no flags). `PanelFlyout` keeps the FULL `p.tools` list. SOLID drops from ~65 inline buttons to ~30.\n- `.panel-tools`: replace the 2-row column-flow grid (index.css:81-84) with single-row flex; `.tool-btn` → 40×52px, icon `size={28}` (Ribbon.tsx:52), drop the `big` 2-row branch and `.tb-label` (label stays in tooltip/`title`). Icon tint `color:#3b6e8f` (Fusion blue-tinted line icons).\n- Belt-and-braces: `.ribbon-panels { overflow-x: auto }`.\n\n### 1.3 Group dropdown: drops DOWN, dividers, submenus, shortcut column\n**File:** `src/components/Ribbon.tsx` PanelFlyout (:58-87)\n- Change `bottom:'100%'`+`marginBottom:4` → `top:'100%'`+`marginTop:4` (drops below ribbon over the canvas, per verified Fusion).\n- Render `sep` items as 1px divider (`#e6e9ee`, margin 4px 6px). Render `children` rows with right-aligned ▸; `onMouseEnter` sets local `subOpen`, nested absolute menu at `left:'100%', top:0`.\n- Keep existing icon + label + right-aligned `<kbd>` shortcut rows (already Fusion-correct). Move the inline style blob into CSS classes `.panel-menu` / `.panel-menu-item` / `.panel-menu-divider`.\n- Filter parents-with-children and `sep` out of the inline quick row.\n\n### 1.4 Fusion group order + CONFIGURE\n**File:** `src/ribbon.ts` (:10-115)\n- Reorder SOLID panels to exactly: **CREATE | MODIFY | CONFIGURE | CONSTRUCT | INSPECT | INSERT | ASSEMBLE | SELECT** (ASSEMBLE moves from 3rd to 7th).\n- Add `CONFIGURE: { name:'CONFIGURE', tools:[{ id:'params', label:'参数', icon:'default', quick:true, tip:'用户参数（命名变量驱动尺寸）' }] }` — reuses the existing `params` command id from MANAGE (:192), so the group is functional.\n\n### 1.5 Big workspace selector \"设计 ▾\"\n**File:** `src/components/Ribbon.tsx` (:234-247), `src/index.css` (:61-71)\n- Restructure `.ribbon` to `display:flex`: left = new `<WorkspaceSelector/>` button spanning tabs+panels rows (Fusion ~160×90; use width 96px × full ribbon height ~108px); right = column of `.ribbon-tabs` + `.ribbon-panels`.\n- `WorkspaceSelector`: `<button className=\"ws-big\">` — small glyph, \"设计\" label, ▾; `useState` dropdown listing 设计 (active, ✓) + greyed 渲染/动画/仿真/制造 entries (`title=\"未实现\"`). Replaces the dead `<div className=\"workspace\">设计 ▾</div>` (Ribbon.tsx:236); delete `.workspace` chip CSS (index.css:66-70) and `ws-sep` (Ribbon.tsx:237).\n\n### 1.6 Fusion light theme tokens\n**File:** `src/index.css` (:1-18, :72-77, :120, :203), `src/components/Viewport.tsx` (:258-259)\n- `--accent: #1a73e8`; `--hover:#e8f0fe`; add `--select-orange:#f0a020`.\n- Selection goes ORANGE: `.tree-row.sel { background:#FDEBD0 }`, `.tl-chip.sel { background:#f0a020; border-color:#e09010 }`, 3D selected emissive `#7a4a00` → `#b35900` @ 0.35.\n- Active ribbon tab = dark, not blue: `.ribbon-tab.active { color:#222; border-bottom-color:#333 }` (verified Fusion: dark underline). Tabs become `<button>` elements.\n- Hygiene: delete dead `src/App.css` (unreferenced Vite template).\n\n**Phase 1 verification** (`preview_start` → `preview_screenshot` at 1920×1080 and 2560×1440):\n1. SOLID tab shows exactly 8 groups left→right CREATE…SELECT, each ≤7 quick icons in ONE row, group label \"CREATE ▾\" beneath — nothing clipped at 1920px; INSPECT/INSERT/ASSEMBLE/SELECT visible.\n2. Click \"CREATE ▾\" → menu drops DOWN over canvas; has divider lines, `阵列 ▸` submenu opens on hover, shortcuts (E/H/Q/F/M/A) right-aligned.\n3. Far-left big \"设计 ▾\" button spans both ribbon rows; clicking opens workspace list.\n4. Active tab underline is dark; select a body → highlight is orange in tree + timeline + 3D.\n5. `preview_eval`: `document.querySelectorAll('.panel-tools .tool-btn').length` ≈ 30 for SOLID; run a non-quick command (e.g. 圆环 torus) from the CREATE dropdown to prove zero feature loss.\n\n---\n\n## Phase 2 — Contextual SKETCH tab (Fusion FORM/SKETCH pattern)\n\n### 2.1 SKETCH panels in the ribbon data model\n**File:** `src/ribbon.ts`\n- Add `export const SKETCH_PANELS: Panel[]`:\n  - **CREATE**: `sk_line`,`sk_rect`,`sk_crect`,`sk_circle`,`sk_circle3`,`sk_arc`,`sk_polygon`,`sk_slot`,`sk_arcslot`,`sk_rrect`,`sk_ellipse`,`sk_spline`,`sk_project` (labels/shortcuts L/R/C/A per existing App.tsx:118-130 map; quick: line/rect/circle/arc/polygon/spline).\n  - **MODIFY**: `sk_trim`,`sk_filletc`,`sk_chamferc`,`sk_offset`,`sk_mirror`,`sk_patternrect`,`sk_patterncirc` (handlers exist at Viewport.tsx:1524-1533, CSketch.tsx:148-151).\n  - **CONSTRAINTS**: one tool per CSketch `CBTN` entry — move the `CBTN` table from `CSketch.tsx:18-39` into `ribbon.ts` so ribbon + CSketch share it.\n  - **SELECT**: `sk_select`.\n- **File:** `src/store.ts` `runCommand` (~:2587): add `sk_*` cases → `setSketchTool(...)` when `mode==='sketch'`, or `useCSketch.getState().setTool(...)`/`apply(kind)` when `csketchOpen`.\n\n### 2.2 Ribbon swap + FINISH SKETCH green check\n**File:** `src/components/Ribbon.tsx`, `src/index.css`\n- `const inSketch = useApp(s => s.mode==='sketch' || s.mode==='pickplane' || s.csketchOpen)`.\n- When `inSketch`: tabs row renders a single highlighted contextual tab `草图` (`.ribbon-tab.ctx { background:#fdebd0 }`) instead of `WORKSPACE_TABS`; panels row renders `SKETCH_PANELS`; pinned at flex-end a green-check button:\n  `<button className=\"finish-sketch\">✓ 完成草图</button>` → calls `finishSketch()` (store.ts:1445) or new store action `finishCSketchFromRibbon()` when `csketchOpen`.\n  CSS: `.finish-sketch { margin-left:auto; min-width:84px; height:61px; display:flex; flex-direction:column; align-items:center; color:#2e7d32; }` with 24px ✓ glyph (Fusion: green check \"FINISH SKETCH\" far right — same pattern as FINISH FORM).\n- **Store refactor:** extract the `finish` closure from `CSketch.tsx:112-122` into the `useCSketch` store; move component-local `height`/`op` (CSketch.tsx:48-49) into that store so the ribbon button can commit.\n- Keep `SKETCH_OK_CMDS` greying (Ribbon.tsx:34,42) only for `pickplane` mode.\n\n### 2.3 Slim the in-canvas sketch bar; CSketch under chrome\n**Files:** `src/components/Viewport.tsx` (:1469-1558), `src/components/CSketch.tsx`, `src/App.tsx` (:155), `src/index.css` (:234), new `src/components/SketchPalette.tsx`\n- `sketch-bar` shrinks to STATUS-ONLY: plane select, cursor coords, snap select, base-offset, area/perimeter readout, undo/redo. All 13 draw tools + fillet/chamfer/offset/mirror/trim move to the ribbon tab. Remove the old in-bar `完成草图` (Viewport.tsx:1556).\n- **Separate sketch from extrude (Fusion behavior):** delete the inline op new/cut/intersect + height/symmetric/twist controls (Viewport.tsx:1511-1521) — they duplicate the Extrude dialog (:1560-1600). CSketch \"完成并拉伸\" → finish commits profiles to `useApp.sketchProfiles`, closes overlay, opens the Extrude dialog (keep `commitCProfiles` as fallback).\n- **CSketch becomes in-viewport:** move `{csketchOpen && <CSketch/>}` from App.tsx:155 into Viewport's root div; `.csketch { position:absolute; inset:0; z-index:30 }` (was `fixed; z-index:50`) so ribbon/browser/timeline/ViewCube stay visible with the SKETCH tab active.\n- **SketchPalette** (new, Fusion's docked Sketch Palette): `position:absolute; right:12px; top:60px; width:216px` card; collapsible sections 「选项」(construction toggle, snap, plane/baseZ, 看向草图), 「约束」(CBTN icon grid, csketch only), 「尺寸」(relocate `.cs-dims` list from CSketch.tsx:250-270). Mounted when `mode==='sketch' || csketchOpen`.\n- Replace CSketch's `window.prompt()` calls (CSketch.tsx:148-151) with a generic `MiniParamPop` anchored popover (number fields + 确定/取消) for chamfer/rect-pattern/circ-pattern/rotate.\n\n**Phase 2 verification:**\n1. Click 创建草图 → pick plane → ribbon swaps to single 草图 tab with CREATE/MODIFY/CONSTRAINTS/SELECT groups + green ✓完成草图 at far right; SOLID tabs gone.\n2. Draw a rect from the ribbon tool, click ✓完成草图 → ribbon restores SOLID; press E → Extrude dialog opens with the profile; commit → body appears, timeline gains chips (no feature loss).\n3. Open constraint sketch → browser tree, timeline, ViewCube all still visible behind it; apply a constraint from the ribbon CONSTRAINTS group; finish from the ribbon check.\n4. `preview_screenshot`: sketch-bar is now a thin status strip; no draw-tool buttons in it.\n\n---\n\n## Phase 3 — App bar + Browser/Timeline polish\n\n### 3.1 Top app bar rebuild (Fusion: home, data panel, File ▾, save, undo ↶, redo ↷ + centered doc name)\n**File:** `src/components/Ribbon.tsx` (:139-231), `src/index.css` (:33-58), `src/icons.tsx`\n- `.topbar { height: 44px }` (Fusion ~56px; 44 reads right at our 13px font — bump to 48-56 if strict).\n- **Keep LEFT only:** home(fit), NEW data-panel grid icon button (opens simple project-list popover or inert), File menu button, save, undo, redo. Add `grid`/`folder` icon paths to icons.tsx.\n- **New `FileMenu` dropdown** (PanelFlyout pattern, `top:'100%'`): 新建 / 打开 / 保存 (Ctrl+S) / ─ / 导入STL / 导入STEP / ─ / 导出 STL / STEP / 装配STL / glTF / ─ / 工程图 / 清空 — handlers moved verbatim from Ribbon.tsx:154-210. Rows = icon + label + right-aligned shortcut.\n- **CENTER doc tab:** orange CUBE icon (`<ToolIcon name=\"box\" size={12} style={{color:'#f0a020'}}/>`, replaces the `.dot` at :149) + live `{projectName || '未命名'}*`; click-to-edit (move the standalone name `<input>` from :141 into the tab; delete the duplicate).\n- **Relocate the ~20 stranded controls (zero loss):**\n  - hole-type select + 孔Ø (:211-216) → into the Hole command dialog (Viewport.tsx:1187-1272 already has these fields — delete topbar copies).\n  - fillet-edge-scope (:217-222) → Fillet dialog (Viewport.tsx:1125-1169).\n  - material select + color swatch (:223-227) → Appearance flow / component context menu (3.3).\n  - 切割 cut-mode toggle (:151) → MODIFY flyout next to combine.\n  - template select+载入 (:169-191) and FastenerPicker (:9-30, :192) → ribbon SOLID INSERT entries (`loadsample`, `insertfastener`) opening small anchored popovers (extract FastenerPicker into its own popover component).\n  - ƒx参数 → already in ribbon (`params`); 🔍 / ? / user avatar stay top-right.\n\n### 3.2 Browser header + doc root + Document Settings/Units + Named Views + Origin\n**Files:** `src/components/BrowserTree.tsx`, `src/store.ts`, `src/icons.tsx`, `src/App.tsx` (:144-147), `src/components/Viewport.tsx`\n- **Header (:189):** `◂◂ 浏览器` + right-aligned ─; click → store `browserCollapsed: boolean`; `.main` grid-template-columns `248px 1fr` ↔ `28px 1fr` (collapsed renders slim `▸▸` strip).\n- **Section icons:** add `icon?: string` prop to `Section` (:34,41); icons.tsx gains `gear`, `folder`, `skipstart`, `skipend`; 文档设置 gets `gear`, 命名视图/原点 get `folder`.\n- **Doc root row (:190):** dedicated row `[👁 global visibility][radio active-doc][orange cube icon][docName*]`; store adds `docName: string`, `dirty: boolean` (set in save/load actions); default label `(未保存)` matching Fusion \"(Unsaved)\".\n- **Document Settings → Units:** make the「单位: mm」Leaf live — onClick cycles/opens the existing `setUnit` store action; label bound to current unit state. Then DELETE the unit `<select>` from vp-toolbar (Viewport.tsx:1758-1760) — Fusion keeps units here, not in canvas chrome.\n- **Named Views (functional):** store adds `savedViews: {name, pos:[number,number,number], target:[number,number,number]}[]`, `captureView()`, `gotoNamedView(i)` (camera pose via the existing ViewRig/`setView`/`requestFit` plumbing, Viewport.tsx:1071). Tree children: 主视图/前/上/右/等轴 (call `setView`+`requestFit`) + user views + 「＋保存当前视图」. Persist in the localStorage doc save. Then DELETE 前/上/右/等轴 text buttons from vp-toolbar (:1750-1753).\n- **Origin group:** add 原点 point Leaf (icon `point`, already in icons.tsx); add 👁 visibility on the group (store `originVisible: boolean`; Viewport renders translucent plane quads when on). Plane-click-to-sketch behavior kept (move to double-click or keep ✎ glyph).\n- **Tree de-clutter (Fusion rows = radio/eye/icon/name only):** `CompRow` (:74-116) keeps `[👁][icon][name]`; material select, color swatch, and 🎯◎⧉⇋⇅🗑 glyphs move into an `onContextMenu` menu (reuse the marking-menu overflow list container from Phase 4): 重命名/孤立/聚焦/复制/镜像/材质▸/颜色/删除. CompBatchBar + 自动配色/显示全部/导出zip move to the Section-header context menu (kept, not deleted).\n- **Sketches folder; features OUT of tree:** delete the 特征 Section (:248-262 — history belongs to the timeline only); add `<Section icon=\"folder\" label=\"草图\">` backed by new store state `sketches: {id, plane, featureId}[]` pushed on every `finishSketch` (minimal v1: list sketches associated with features; eye toggles sketch-line visibility read by Viewport).\n\n### 3.3 Timeline → Fusion bottom-left strip\n**Files:** `src/components/Timeline.tsx`, `src/index.css` (:198-231), `src/App.tsx` (:148)\n- **Placement:** move `<Timeline/>` inside the viewport container; `.timeline { position:absolute; left:8px; bottom:8px; width:auto; max-width:55%; border:1px solid var(--border); border-radius:4px; background:rgba(255,255,255,.92); overflow-x:auto }` — bottom-LEFT compact strip, bottom-center stays free for the navbar. Verify no overlap with `.vp-props` (collapse it per Phase 4.5).\n- **Playback controls (:61-68):** replace undo/redo glyphs on the ⏮/⏭ keys with new `skipstart`/`skipend` icons; play button toggles ⏸ while playing (local `playing` state + ref check in the 320ms loop); `n/total` counter only shown when rolled back (or in marker tooltip).\n- **Chips → Fusion icon squares (:80-89):** 26×26px icon-only chips (`<ToolIcon name={m.icon} size={16}/>`), label+params in `title` tooltip; cut operations get a small red corner badge. **Interaction split:** single-click = `selectFeature(f.id)` only; double-click = open editor; suppressed style kept.\n- **Draggable position marker (:71):** move the static `.tl-marker` into the chips loop at `i === timelinePos`; make it a 4px handle with `cursor: ew-resize` + 4px hot zones; `onPointerDown/Move` computes drop index from chip `getBoundingClientRect` midpoints, throttled `gotoStep(idx)`; `onPointerUp` ends. This is THE Fusion scrub interaction.\n- **Feature editor (:95-146):** migrate `feat-editor` horizontal strip into the Phase-4 `CommandDialog` right-side palette (META field defs reused verbatim; flow: double-click chip → roll to step → palette → 确定 → `gotoStep(length)`). ◀▶ reorder moves to chip right-click 上移/下移 (or drag).\n\n**Phase 3 verification:**\n1. Screenshot top bar: ≤7 left icons + centered doc tab with orange cube + name; no export/template/fastener/hole/material controls anywhere in the topbar; File ▾ opens with dividers and every import/export entry works (export an STL).\n2. Browser: header `◂◂ 浏览器` collapses/expands the 248px panel; tree reads (eye)(radio) 未命名* → ⚙文档设置(单位: mm clickable) → 📁命名视图 (click 前 → camera fronts; ＋保存当前视图 adds a row that restores pose) → 📁原点; no 特征 section; component rows show only eye/icon/name, right-click shows the old actions.\n3. Timeline: bottom-left strip, icon-only squares, ⏮◀▶⏭ glyphs correct, marker sits after the last chip and DRAGS to scrub history; double-click a chip opens the right-side editor palette.\n4. Regression `preview_eval`: change a unit via tree, restore a named view, suppress + reorder a feature via context menu — all still functional.\n\n---\n\n## Phase 4 — Dialogs + navbar + marking menu + S-key\n\n### 4.1 Shared `CommandDialog` shell (Fusion palette anatomy)\n**New file:** `src/components/CommandDialog.tsx`\n- Props: `{ icon?, title, okLabel?, okDisabled?, onOk, onCancel, width?, summary?, children }`.\n- Anatomy: fixed `top:150; right:20; width:240` white card (styles verbatim from Viewport.tsx:1561); **header** = `<ToolIcon/>` 18px + bold title + ✕ close, draggable (pointer capture); **body** = children rows (`display:flex; justify-content:space-between` like hole rows :1199-1202); **footer** = right-aligned blue `确定` (#1572c4→`var(--accent)`, `okDisabled` support) + `取消`; optional grey summary line. Enter/Esc global handling (App.tsx:72-102) unchanged.\n- Export `SelectionChip({ label, count, hint, onClear })`: Fusion's selection chip — count>0 → blue pill `已选 N ×` (bg #d8eafa, ✕ clears); else grey dashed hint pill.\n- **Refactor existing 6 palettes** (extrude :1560, edge-round :1125, shell :1171, hole :1187, sweep :1602, loft :1628) into it; replace prose status lines with chips: edge-round → `edgeRoundPicks.length` + new store action `clearEdgeRoundPicks`; shell → `shellPicks`; hole → `holePos?1:0`; extrude/loft → read-only Profile/截面 chips.\n\n### 4.2 featDlg → right-side palette (~25 commands)\n**File:** `src/components/Viewport.tsx` (:1274-1456)\n- Replace the top-center `.sketch-bar` strip container with `<CommandDialog icon title={map[featDlg.kind]} onOk={commitFeatDlg} onCancel={cancelFeatDlg} width={250}>`; every per-kind `<label>` block kept verbatim in a column wrapper. PatternPreview (:354) unaffected.\n- Replace the component-strip `window.prompt` array/circular-array/scale (:1744) with featDlg kinds `comp-array`/`comp-circ`/`comp-scale` committing to existing `arrayComponent`/`circArrayComponent`/`scaleComponent`.\n\n### 4.3 Real bottom-center navbar + dissolve `.vp-toolbar`\n**Files:** `src/components/Viewport.tsx` (:1749-1828), `src/store.ts`, `src/ribbon.ts`, `src/components/Ribbon.tsx`\n- **Navbar (:1823-1828)** becomes Fusion's 6 controls:\n  - orbit/pan/zoom → live buttons setting store `navTool:'orbit'|'pan'|'zoom'` (default 'orbit'), mapped to OrbitControls `mouseButtons` (:1083); active styling; fix zoom's wrong 'measure' icon.\n  - fit (keep).\n  - `显示设置 ▾` upward popover: store `visualStyle:'shaded'|'shadedEdges'|'wireframe'` (wireframe flag / `<Edges>` mount in ShapeView), `cameraOrtho: boolean` (swap camera).\n  - `网格设置 ▾` upward popover: `showGrid: boolean` (wrap drei Grid :1045), snap increment (reuse `snapSize`).\n- **Dissolve the top-left junk-drawer `.vp-toolbar` (:1749-1821)** — Fusion has NO persistent in-canvas command bar:\n  - DELETE duplicates already in ribbon: 面草图, 选边圆角/倒角, 量边/量面/量角 (ribbon.ts:15,56,57,90-93).\n  - ADD missing ribbon entries: `{ id:'pushpull', label:'按拉', shortcut:'Q', quick:true }` in MODIFY → existing togglePushPull; 14 工程计算 calculators → INSPECT dropdown tools; 📷图片 PNG export → File menu.\n  - MOVE 剖视 section controls (axis/offset/capped/flip) → INSPECT `section` command opening a CommandDialog; explode/x-ray/堆叠/排版/全落地 → ASSEMBLE group tools; unit select already moved to tree (3.2); view buttons already moved (3.2).\n  - Result: delete the entire `.vp-toolbar` div.\n- **ViewCube home icon:** absolute 🏠 button at `right:148, top:14` (left of GizmoHelper margin [78,92]) → `setView('iso'); requestFit()`.\n\n### 4.4 Radial marking menu\n**New file:** `src/components/MarkingMenu.tsx`; **File:** `src/components/Viewport.tsx` (:1094-1123), `src/store.ts`\n- Keep trigger/right-drag discrimination (:914-920) and Esc/backdrop logic; replace the list renderer: 8 circular ~56-64px buttons at 45° steps, radius ~70-86px around (x,y), icon + 2-word label; remaining items as the existing vertical list BELOW (y+90) — Fusion's radial + overflow-list pattern.\n- Model-mode sectors: N=重复上次 · NE=创建草图 · E=按拉 · SE=删除 · S=移动/复制 · SW=圆角 · W=适应窗口 · NW=测量. Sketch-mode: 重复上次/直线/矩形/圆/完成草图/拉伸…/撤销/删除.\n- Store: `lastCommand: {id,label}|null` set at top of `runCommand`; N sector replays it (greyed when null).\n\n### 4.5 S-key shortcuts box + status consolidation\n**Files:** `src/App.tsx` (:57,119), `src/components/CommandPalette.tsx`, `src/components/Viewport.tsx`\n- `case 's'` → `setCmdPalette(true)` (Fusion's S shortcuts box); keep `/` alias; sketch stays on ribbon button + `Shift+S`; update ribbon.ts:14 tooltip and the 🔍 tooltip (Ribbon.tsx:228).\n- CommandPalette → compact Fusion box: width 360, `paddingTop:'18vh'`, rows = `<ToolIcon size={16}/>` + label + `marginLeft:'auto'` kbd; source tag → tooltip.\n- Status: `.vp-badge` moves bottom-center-left prefixed with current command name; `.vp-props` mega-strip collapses by default to one line (dims·volume·mass) with `≡ 详情` expander (local state).\n- **Stretch (flagged, separate commit):** extrude on-canvas manipulator — arrow mesh at profile centroid in `SketchLayer.tsx` ExtrudePreview (:296+), pointer-drag raycast onto the normal axis → `setExtrudeHeight`, drei `<Html>` inline distance input at the tip (or translation-only TransformControls proxy). This completes Fusion's ghost+arrow+inline-textbox triad.\n\n**Phase 4 verification:**\n1. Open 旋转/阵列/移动 etc. → ALL appear as right-side palettes (no top-center strips); header icon+title+✕, draggable; fillet shows blue `已选 N ×` chip that clears picks; OK/Cancel blue footer.\n2. Canvas top-left is EMPTY (no vp-toolbar); navbar = orbit/pan/zoom (clicking pan makes left-drag pan)/fit/显示设置▾(wireframe toggles live)/网格设置▾(grid hides); 🏠 beside ViewCube homes the view.\n3. Right-click canvas → 8-way radial + overflow list; run extrude, right-click → N=重复上次 re-opens extrude.\n4. Press S → compact shortcuts box with icons; type 圆角 → row icon+label+kbd F; Shift+S starts sketch.\n5. Regression: every former vp-toolbar command reachable (按拉 Q, 剖视 dialog, explode slider in ASSEMBLE, calculators in INSPECT dropdown, PNG export in File menu); component array/scale no longer uses `window.prompt`.\n\n---\n\n## Risks & mitigations\n\n| Risk | Detail | Mitigation |\n|---|---|---|\n| **Viewport.tsx is a monolith (~2000 lines)** | Phases 2–4 all edit it; vp-toolbar deletion touches state wired across the file (section/explode/measure flags) | Do moves command-by-command; grep each deleted control's store field for remaining consumers before removing UI |\n| **Keyboard conflicts** | S currently = sketch (App.tsx:119); sketch one-key tools S/L/R/C/A (:122-129) vs new S=palette | Rebind in one commit with CommandPalette change; guard all single-key handlers on `!inputFocused && mode` checks; document Shift+S |\n| **`SKETCH_OK_CMDS` gating vs contextual tab** | Once the ribbon swaps in sketch mode, the whitelist becomes mostly unreachable but `pickplane` still needs it | Keep the gate for `pickplane`; remove only after Phase 2 verification |\n| **CSketch state extraction** | Moving `finish`/`height`/`op` from component state into `useCSketch` risks breaking the transient-reset behavior (CSketch.tsx:54) | Keep the reset-on-open; add a store-level finish that mirrors the old closure exactly; keep `commitCProfiles` fallback |\n| **Top-center slot collisions** | `.sketch-bar` class is reused by featDlg, pick-plane hint, component strip | Phase 4.2 removes featDlg from it first; keep pick-plane hint; component strip migrates to featDlg kinds |\n| **Timeline ↔ vp-props overlap** | Both land bottom-left after Phase 3.3 | Collapse vp-props to one line (4.5) and verify at 1366px width screenshot |\n| **Dropdown drop-down direction** | Menus now overlay the canvas; must close on outside-click and not block OrbitControls | Backdrop div + `pointer-events` audit; verify orbit works with a menu open then closed |\n| **No feature loss is a hard requirement** | ~20 topbar controls + ~30 vp-toolbar controls relocate | After each phase run the regression list in that phase's verification; CommandPalette (now on S and /) is the safety net — every command id stays registered |\n| **CONFIGURE/FORM authenticity** | CONFIGURE has 1 tool; FORM not built | CONFIGURE wired to real `params`; FORM intentionally omitted — note in DEVLOG |\n\n## Execution order & ship gates\n1. **Phase 1** (ribbon.ts + Ribbon.tsx + index.css, ~1 day) — ship alone; biggest visual delta, smallest blast radius.\n2. **Phase 2** (ribbon swap + CSketch integration) — ship after sketch round-trip regression passes.\n3. **Phase 3** (topbar/browser/timeline) — 3.1 / 3.2 / 3.3 are independently shippable commits.\n4. **Phase 4** (CommandDialog → featDlg → navbar/vp-toolbar → marking menu → S-key → manipulator stretch) — S1→S2 first (highest payoff), manipulator last.\n\n**Every phase ends with:** `npm run build` in `C:\\ClaudeCode\\webcad` + VPS deploy (per `deploy-webcad-after-changes.md` — the job is not done until live), then a `preview_screenshot` of the deployed URL compared against the verified Fusion reference shots (2560×1440 light theme): app bar height/content, ribbon group order + quick-icon counts, dropdown anatomy (shortcut column/dividers/submenus), contextual-tab + green check, browser tree first 4 rows, timeline bottom-left, navbar 6 controls, radial menu, S-box."
}