# WebCAD Agent Instructions

## FrameLens：UI／圖片／影片檢查優先

凡涉及 UI 截圖、畫面布局、視覺回歸、圖片或影片分析，必須先使用
FrameLens；只有 FrameLens 無法提供足夠證據時，才補充其他檢查方式。

- 本機 CLI：`C:\tmp\framelens-0.1.0\FrameLens\.venv\Scripts\framelens.exe`
- 先跑 `framelens doctor` 確認後端；圖片用 `framelens image <path>`，影片用
  `framelens video <path-or-url>`。
- FrameLens 的 JSON 輸出應用於快速找出文字、溢位、重疊、錯誤提示及畫面差異；
  必要時再以瀏覽器／畫布實機互動確認。
- 目前 OCR、ffmpeg、yt-dlp 可用；未有 Ollama vision model 時，不能聲稱已做 VLM
  語義判讀。

## 功能驗收標準（使用者要求，2026-09-06）

使用者最新指定組別順序：**1 草圖 → 2 實體交叉操作 → 5 介面／操作 → 6 工程圖 → 3 Create Form → 4 網格轉換 → 7 CAM／裝配／模擬**。按此順序推進；修正本組時所需的介面與回歸驗證同步做。目標是對照 Fusion 360 達到 90% 以上接近程度，必須先建立具體功能與操作驗收矩陣，未驗證不得計入達成；測試通過率不是功能接近率，不得以縮小清單或把未接通入口算完成來達標。

新增功能、修正及尚未驗收的常用功能，必須按交叉使用情境驗收；單一命令成功、非空網格或純 UI 狀態測試不足以表示功能完成。

使用者已授權多 sub-agent 及分層並行驗證。每個 agent 指定獨立編輯範圍，避免同改 store／同一測試；昂貴測試 benchmark 只由一個 owner 執行。L0 每次修正先跑直接重現及相關測試；L1 驗受影響的幾何交叉流程、歷史／儲存及 UI；L2 一批整合完成後跑完整套件及建置，不為每個小改動反覆跑所有無關模擬。不得把 L0/L1 當作完整驗收。

`npm run test:all` 使用有界並行（預設 2），共用 `.tmp-fea` 的兩個測試保持獨立串行階段；兩段 Node 摘要的測試數必須相加。`npm run test:all:serial` 保留診斷用串行。

使用者明確要求 **同時開三個 WebCAD、三線測試**。瀏覽器 runner 預設 concurrency=3，smoke 包含 dimension、projection、arc 三路。`node examples/run-browser-regressions.mjs --groups=smoke --headed` 顯示三個獨立 Chrome 視窗；省略 `--headed` 則在背景運行同樣三個瀏覽器。每路使用獨立 profile、模型及證據目錄，不接管用家已有頁面。保留實際耗時，不把不同工作組的耗時當作同條件速度增益。功能源碼寫入與 HMR 須在短測試期間協調，避免重載污染結果。

- 常用建模優先：草圖／約束與尺寸 → 拉伸／旋轉／切割 → 面上與不同基準面再繪圖 → 圓角／倒角 → 布林 → 孔／抽殼／拔模／鏡像／陣列 → 掃掠／放樣；完成常用覆蓋後再擴展進階工具。
- 在已有特徵的模型上使用功能，之後再疊加其他功能；至少一組改上游尺寸、Undo／Redo、專案儲存重開，適用時 STEP 往返。
- 核對實際體積／尺寸或其他獨立預期、OCCT B-rep 有效性、有限座標，並驗證失敗不損壞模型及歷史；不能偷偷縮小參數或無變化卻報成功。
- 對有 UI 入口的修改，實測滑鼠、數值輸入、快捷鍵及 Esc；一個 Esc 只取消最上層操作，保持草圖／模型／視角，長按不得連環退出。
- 顯示測試使用同一混合模型交替切換著色、線框、隱藏虛線、半透明、網格、渲染及剖面；確認顯示不改幾何、剖面邊與面一致，功能不是只有選單名稱。
- 小視窗、矮視窗、正常桌面檢查控制項可見、可捲到且可點擊，遮擋時可收起／移開；自動化不得冒充真人 IME／觸控板／實際浏览器縮放驗收。
- 報告明列已驗證、有限制及未驗證項目；每次只把有證據通過的功能列為完成，不聲稱全面 Fusion 對等。

## 正式網站版本（使用者要求，2026-09-07）

頁面頂部必須顯示產品 version。首次為 1.0，來源 src/version.ts；此產品版本與 JSON schema version 分開。之後每次正式發布，執行一次 npm run release:build，自動遞增為 1.1、1.2 等；同次發布重試只用 npm run build，不重複加號。部署前比對線上版本，確保新發布版本較大；驗證後備份並切換站點，記錄版本及校驗碼。不得把日常測試建置當作一次發布。

## 效率與證據（2026-09-08）

依 OpenAI 官方 best practices（https://learn.chatgpt.com/guides/best-practices）：每個缺陷先明列重現資料與完成條件；只讀相關片段，完整日誌留 outputs、回傳失敗摘要；獨立子任務才委派，交代檔案所有權及回報格式。測試依 L0→L1→整合關卡遞進，相同程式及關卡已通過就不重跑，除非有新改動或未解風險；建置與重型套件各設一個 owner。長程序等具體 handle，避免密集空輪詢。維持完整功能目標，不以省 Token 取消必要的幾何與真 UI 驗證。

## 可見測試（使用者要求，2026-09-08）

介面測試使用可見瀏覽器頁面，讓使用者同步觀看；幾何與非介面自動測試保持背景執行。無須為展示加入人工延遲，避免降低速度。保留使用者原頁面，另開測試頁。
